# Core dependencies with pinned versions
# Based on setup instructions in README.md

# Transformers - must be <4.46 to avoid BeamSearchScorer issues with TTS
transformers==4.45.2

# PyTorch - exact versions to avoid weights_only compatibility issues
torch==2.5.1
torchaudio==2.5.1

# Text-to-Speech
TTS==0.22.0

# Gradio UI framework
gradio>=4.0.0,<5.0.0

# Speech-to-Text
faster-whisper>=1.0.0,<2.0.0

# Audio processing
soundfile>=0.12.0

# Numerical operations
numpy>=1.24.0,<2.0.0

# HTTP requests for LLM API calls
requests>=2.31.0

# Optional: for development
# mypy>=1.0.0  # Type checking
# black>=23.0.0  # Code formatting
# ruff>=0.1.0  # Fast linting
# pytest>=7.0.0  # Testing
